# アフィリエイトスロット一覧（トレ飯相棒）

物販導線の実体は `index.html` 内の `SHOP` 配列。各リンクは `data-aff` 属性でスロット識別。

| スロット | data-aff | 商品 | 現在のURL | 差し替え先 |
|---------|----------|------|-----------|-----------|
| プロテイン | `RAKUTEN_PROTEIN` | ホエイプロテイン | 楽天検索ページ（素リンク） | 楽天アフィリエイトURL発行後、`SHOP[0].rakuten` を差し替え |
| シェイカー | `RAKUTEN_SHAKER` | プロテインシェイカー | 楽天検索ページ（素リンク） | 同上 `SHOP[1].rakuten` |
| スケール | `RAKUTEN_SCALE` | キッチンスケール | 楽天検索ページ（素リンク） | 同上 `SHOP[2].rakuten` |

## 運用ルール

- **URL差し替えは `SHOP` 配列の `rakuten` / `amazon` の1行だけ**。`amazon` にURLを入れると自動でAmazon優先表示（もしも承認後）
- URLが両方空のスロットは自動で「準備中」表示になりリンク切れしない
- 全リンク `rel="sponsored noopener nofollow"` `target="_blank"` 固定
- PR表記：ショップ画面冒頭＋文脈プロモ枠に `PR` タグ表示済み（消さない）

## 掲載面

1. **ショップ画面**（`#v-shop`）… 設定画面のボタン＋ホーム下部の入口カードから遷移
2. **文脈プロモ枠**（ホーム `#promo-slot`）… 直近3日中2日以上「記録ありでP未達」の時だけ1枠表示。×で却下可

---

## 🔴 楽天リンクを追加するときは必ず「計測ID」を入れる（2026-09-03 制定）

このサイトの計測ID＝**`_RTLink143607`（meshi）**。

形式（アフィリID の直後にパスセグメントとして入れる）：

```
https://hb.afl.rakuten.co.jp/ichiba/{アフィリID}/_RTLink143607?pc={URLエンコードした商品URL}&link_type=text
```

- `/hgc/` 形式でも同じ位置に入れれば有効（302 で `/ichiba/` へ引き継がれる。2026-09-03 実測）
- **これが無いと楽天のサイト別レポートに載らず、どのサイトの成果か永久に分からなくなる**
- 全サイトのID対応表と発行手順＝`~/Desktop/claude/affiliate_links.md` の「楽天 計測ID（site_pointback_id）」節

## 🔴 utm を付けてよい条件（2026-09-13・正本＝`~/Desktop/claude/memory/analytics_snapshot.md` §2-B）

判定軸は「リンクをどこに置いたか」ではなく **「着地先が自分のGA4プロパティかどうか」**。

- ✅ 付ける＝**外部の面へ送る**リンク（note/Amazon/Gumroad…）／**外部から自社サイトへ**送るリンク／**別ドメインの自社サイト**へ送るリンク
- ❌ 付けない＝**同じGA4プロパティ内の内部リンク**／**リダイレクトの着地先**（`/短縮 → /実体?utm_...`）
- 理由＝GA4 はセッションの帰属を `session_start` が運ぶ utm で決め、**本当の参照元を上書きする**。
  「不要なリファラー」設定は `ignore_referrer` を付けるだけで **utm には効かない＝後から救えない**
- 検査＝`grep -rn "utm_" --include="_redirects" .` が **0行** ／ `curl -sI <短縮URL>` の `location` に `utm_` が無い
- 実害＝2026-09-07、宅建GYMの最多クリック面 `/note`（自然検索 5クリック/41表示）が着地で utm を着せ、
  **自然検索の流入がGA4上で「note経由」に化けていた**（2026-09-13 時点で**未修正**）

## 点検ログ（週次・SHIP-03）

> 書式＝`YYYY-MM-DD｜実数 n／表 n｜差分`。**書かない週＝SHIP-03 が止まった週**として人事が数える。

- 2026-09-16｜実数 3／表 3｜差分なし（`data-aff` の grep は1件だが `data-aff="${p.aff}"` のテンプレート1本がJS配列の RAKUTEN_PROTEIN／SHAKER／SCALE を動的生成している）
